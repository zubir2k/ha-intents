language: "it"
intents:
  HassListCompleteItem:
    data:
      - sentences:
          - (controlla[re] | completa[re] | elimina[re] | rimuov(i|ere) | spunta[re] ) [<the>] <item> [(<from> | <on> | <in>)] <my_list>
        response: item_completed
        requires_context:
          domain: todo
        expansion_rules:
          my_list: "[<my>] [lista] [<of>] {name}"
          item: "{todo_list_item:item}"
