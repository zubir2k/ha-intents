language: pt
intents:
  HassTurnOn:
    data:
      - sentences:
          - "<ligar> [tod(as|os)] [(o[s]|a[s])] <luz> <na_zona>"
        slots:
          domain: "light"

      # Ligar luzes na mesma divisão que um dispositivo de voz
      - sentences:
          - "<ligar> [(o[s]|a[s])] <luz>"
          - "<ligar> [tod(as|os)] [(o[s]|a[s])] <luz> aqui"
        slots:
          domain: "light"
        requires_context:
          area:
            slot: true
