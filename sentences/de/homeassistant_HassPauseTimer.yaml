language: de
intents:
  HassPauseTimer:
    data:
      - sentences:
          - "pausiere[ (den|meinen)] Timer"
          - "pausiere[ (den|meinen)] <timer_start> Timer"
          - "pausiere[ (den|meinen)] Timer für <timer_start>"
          - "pausiere[ (den|meinen)] {area} Timer"
          - "pausiere[ (den|meinen)] Timer <area>"
          - "pausiere[ (den|meinen)] {timer_name:name} Timer"
          - "pausiere[ (den|meinen)] Timer (namens|für) {timer_name:name}"
          - "[(den|meinen) ]Timer (pausieren|anhalten)"
          - "[(den|meinen) ]<timer_start> Timer (pausieren|anhalten)"
          - "[(den|meinen) ] {area} Timer (pausieren|anhalten)"
          - "[(den|meinen) ]Timer <area> (pausieren|anhalten)"
          - "[(den|meinen) ]{timer_name:name} Timer (pausieren|anhalten)"
          - "[(den|meinen) ]Timer (namens|für) {timer_name:name} (pausieren|anhalten)"
          # halte an
          - "halt[e] (den|meinen) Timer an"
          - "halt[e] (den|meinen) <timer_start> Timer an"
          - "halt[e] (den|meinen) Timer für <timer_start> an"
          - "halt[e] (den|meinen) {area} Timer an"
          - "halt[e] (den|meinen) Timer <area> an"
          - "halt[e] (den|meinen) {timer_name:name} Timer an"
          - "halt[e] (den|meinen) Timer (namens|für) {timer_name:name} an"
