language: de
responses:
  intents:
    HassMediaNext:
      default: |
        {{ ["Nächstes Element wird abgespielt", "Nächstes Element wird wiedergegeben", "Überspringe aktuelles Element"] | random }}
