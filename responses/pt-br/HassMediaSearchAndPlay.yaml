---
language: pt-br
responses:
  intents:
    HassMediaSearchAndPlay:
      default: |
        {% if slots.media %}
        Reproduzindo mídia
        {% else %}
        Mídia não encontrada
        {% endif %}
